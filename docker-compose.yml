services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mutual-fund-screener
    ports:
      - "80:80"
    env_file:
      - ./backend/.env
      - ./frontend/.env
    restart: unless-stopped
